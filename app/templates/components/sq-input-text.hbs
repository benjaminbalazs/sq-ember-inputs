{{input
		disabled=disabled
		type=type
		value=value
		focus-in="focusIn"
		focus-out="focusOut"
		required=required
		maxlength=maxlength
		dir=dir
		classNames='sq-align-left'
		dasherize=dasherize
		subdomain=subdomain
		domain=domain
		email=email
		number=number
		format=format
}}

<label class="sq-align-left">{{placeholder}}</label>

{{#if after}}

	{{input disabled=true value=after classNames='after' }}

{{/if}}
